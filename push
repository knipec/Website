#!/bin/sh

cd "`dirname "$0"`"

jekyll build || exit

git checkout master || exit
exclude_flags="--exclude=${exclude// / --exclude=}"
rsync -rl --exclude='*.md' _site/* . || exit
git add --all . || exit
git commit || exit
git push || exit

git checkout source || exit
git push || exit
